version: "3.9"


services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: leasing
      POSTGRES_USER: leasing_user
      POSTGRES_PASSWORD: leasing_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
#    api:
#      build: .
#      depends_on:
#        - db
#      ports:
#        - "8000:8000"
#      environment:
#        DATABASE_URL: postgresql+psycopg2://leasing_user:leasing_pass@db:5432/leasing

volumes:
  postgres_data: